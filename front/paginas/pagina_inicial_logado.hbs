<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- ".." vira alias para ../public -->
    <link rel="stylesheet" href="/public/styles/style_pagina_inicial_logado.css">

    <title>Saúde IFRN</title>
    <script>
        function formatDate(dateString) {
            const date = new Date(dateString);

            const day = String(date.getDate()).padStart(2, '0');
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const year = date.getFullYear();

            const hours = String(date.getHours()).padStart(2, '0');
            const minutes = String(date.getMinutes()).padStart(2, '0');

            return `${day}/${month}/${year}, ${hours}:${minutes}`;
        }
    </script>
</head>

<body>
    <header class="header">
        <div class="class nav-left">
            <a href="#">+Saúde IFRN</a>
            <a href="#" style="color:#3BE799">Início</a>
            <a href="/agendamentos">Agendamentos</a>
        </div>
        <div class="class nav-right">
            <a href="/historico">Histórico</a>
            <a href="/perfil">Perfil</a>
        </div>
    </header>
    <div class="container">
        <div class="class left-column">
            <div class="section-consultas">
                <h3>Consulta(s) Pendente(s)</h3>
                <div class="consulta-pendente">
                    <p>Nenhuma consulta pendente.</p>
                </div>
            </div>
            <div class="section-ultimas">
                <h3>Última(s) Consulta(s)</h3>
                <div id="consulta-ultimas">
                    {{#if atendimentos.length}}
                    {{#each atendimentos}}
                    <div class="atendimento">
                        <p><strong>Atendimento ID:</strong> {{atendimento_id}}</p>
                        <p><strong>Horário:</strong> {{atendimento_horario}}</p>
                        <p><strong>Profissional:</strong> {{profissional_nome}}</p>
                        <p><strong>Especialização:</strong> {{especializacao_nome}}</p>
                    </div>
                    {{/each}}
                    {{else}}
                    <p class="sem-atendimentos">Não há últimas consultas.</p>
                    {{/if}}
                </div>

            </div>
        </div>
        <div class="section-avisos">
            <h3>Avisos</h3>
            <div class="avisos">
                <p>{{#if msg1}}{{msg1}}<br>
                    {{/if}}
                    {{#if msg2}}{{msg2}}{{/if}}</p>
            </div>
            <div class="avisos">
                <p>Você não tem novas notificações.</p>
            </div>
            <div class="avisos">
                <p>Você não tem novas notificações.</p>
            </div>
        </div>
    </div>

    <footer class="footer">
        <p>© 2025 +Saúde IFRN. Todos os direitos reservados.</p>
        <p>Entre em contato:</p>
        <a href="https://wa.me/1234567890" class="whatsapp">WhatsApp</a>
        <img src="#" alt="Logo">
    </footer>
    <script src="/public/scripts/script_paginicial.js"></script>
</body>

</html>